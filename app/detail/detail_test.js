'use strict';

describe('AppTrakt.detail module', function() {

  beforeEach(module('AppTrakt.detail'));

  describe('detail controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var DetailController = $controller('DetailController');
      expect(DetailController).toBeDefined();
    }));

  });
});